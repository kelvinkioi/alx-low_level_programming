#include <stdio.h>
/**
 * main - entry point
 * argc: integer
 * argv: array of strings
 * Return: 0 success
 */

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
